<%= form_with(model: check_list_item) do |form| %>
  <% if check_list_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(check_list_item.errors.count, "error") %> prohibited this check_list_item from being saved:</h2>

      <ul>
        <% check_list_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" style="color:white">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="field" style="color:white">
    <%= form.label :text %>
    <%= form.text_field :text %>
  </div>

  <div class="field" style="color:white">
    <%= form.label :check_list %>
    <%= form.select :check_list_id, options_from_collection_for_select(@check_list, :id, :hazard_type)%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
