<% if @contractor.errors.empty? %>
  // add the row to the table, show alert and hide modal
  $('table tbody').append(' <%= j render "row", contractor_type: @contractor_type %>');
  $('.alert-success').html("El Tipo de contratista ha sido ingresada con éxito").show();
  $('#people-modal').modal('hide');
<% else %>
  // show first error
  alert('<%= @contractor_type.errors.full_messages[0] %>');
<% end %>
