<tr id="<%= contractor.id %>">
  <td><%= contractor.id %></td>
  <td><%= contractor.name %></td>
  <td><%= contractor.contractor_types.map {|c| c.name}.join(", ") %></td>
  <td><%= contractor.inspeccions.map {|contractor| contractor.date}.join(", ") %></td>
  <td>
    <%# p=contractor_types.find(contractor.contractor_types_id) %><%#= p.name %>
  </td>
  <td><%= link_to (fa_icon "edit"), edit_contractor_path(contractor), class: "btn btn-secondary btn-sm shadow", title: "Editar contratista", remote: true %></td>
  <td><%= link_to (fa_icon "trash"), contractor, method: :delete, data: { confirm: "¿Estas seguro de eliminar contratista"}, class: "btn btn-danger btn-sm shadow", title: "Eliminar " %></td>
</tr>
