<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @contractor.name %>
</p>
<h2>contractor_type</h2>

<% if @contractor.contractor_types.any? %>
	    <ul>
		    <% @contractor.contractor_types.each do |type| %>
		      <li><%= link_to type, type %></li> 
	      <% end %>
    	</ul>
<% else %>
	      <p>no hay contractor_type</p>
<% end %>

<%= link_to 'Edit', edit_contractor_path(@contractor) %> |
<%= link_to 'Back', contractors_path %>
